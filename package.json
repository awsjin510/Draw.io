{
  "name": "aws-diagram-generator",
  "version": "0.1.0",
  "description": "AWS architecture diagram generator using Claude API and draw.io XML output",
  "type": "module",
  "scripts": {
    "test-api-key": "node src/test-api-key.js",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
